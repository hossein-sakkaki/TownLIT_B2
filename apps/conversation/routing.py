# # apps/conversation/routing.py
# from django.urls import re_path
# from apps.conversation.consumers import DialogueConsumer

# websocket_urlpatterns = [
#     re_path(r"^ws/conversation/$", DialogueConsumer.as_asgi()),
#     re_path(r"^ws/conversation/(?P<slug>[\w-]+)/$", DialogueConsumer.as_asgi()),
# ]
